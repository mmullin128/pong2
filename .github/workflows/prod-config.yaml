name: Server Config
on:
  push:
    branches:
      - config
jobs:
  config:
    runs-on: [self-hosted, prod]
    steps:
      #- name: First Time Startup
      #  run: |
      #    sudo apt update
      #    sudo apt install snapd
      #    sudo reboot
      - name: Verify Snapd
        run: |
          sudo snap install hello-world
      