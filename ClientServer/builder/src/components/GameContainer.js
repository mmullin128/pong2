

export default function GameContainer({ id }) {
    return (
        <div 
            className={`game-container`} 
            id={id}
        >

            <p>Game Container</p>
        
        </div>
    );
}